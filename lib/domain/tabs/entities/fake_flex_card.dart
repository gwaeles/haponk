import 'flex_card.dart';

class FakeFlexCard {
  final FlexCard card;
  final int rowIndex;
  final int itemIndex;

  FakeFlexCard({
    required this.card,
    required this.rowIndex,
    required this.itemIndex,
  });
}
